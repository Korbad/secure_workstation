---
- hosts: localhost
  vars:
    ansible_user: "{{ lookup('env','USER') }}"
    ansible_connection: local
  tasks:
  - name: korbad.project_management.managed_project_manager
    include_role:
      name: korbad.project_management.managed_project_manager
      tasks_from: pull
